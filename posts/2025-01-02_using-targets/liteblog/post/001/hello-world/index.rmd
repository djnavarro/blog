---
title:  Hello World
subtitle: This is my first blog post
---

```{r}
#| label: setup
#| echo: false
source(fs::path(
  rprojroot::find_root(rprojroot::has_file("_targets.R")),
  "scripts",
  "common.R"
))
opts <- get_options()
```

This is a simple blog post. It contains text. It would be weird if it did not.

```{r}
#| label: footer
#| echo: false
#| results: asis
write_footer()
```

